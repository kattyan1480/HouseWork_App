<% content_for :header_title do %>
  過去のやり残し
<% end %><br>

<% if @before_yesterday_chore_dates.any? %>
  <div class="chore-list">

    <!-- ヘッダー -->
    <div class="chore-row-header chore-header">
      <div class="chore-title-header-homes">家事名</div>
      <div class="chore-reward-header-homes">報酬</div>
      <div class="chore-date-header-homes">日時</div>
    </div>

    <% @before_yesterday_chore_dates.each do |chore_date| %>
      <% chore = chore_date.chore %>

      <div class="chore-row js-open-modal"
        data-chore-id="<%= chore.id %>"
        data-chore-date-id="<%= chore_date.id %>"
        data-chore-title="<%= chore.title %>">

        <!-- 左：カテゴリ＋タイトル -->
        <div class="chore-main">

          <!-- 丸アイコン -->
          <div class="chore-icon">
            <%= image_tag chore.category.stamp, class: "category-icon-home" %>
          </div>

          <!-- テキスト -->
          <div class="chore-text">
            <span class="category-badge"
                  style="background-color: <%= chore.category.color %>;">
              <%= chore.category.name %>
            </span>

            <div class="chore-title">
              <%= chore.title %>
            </div>
          </div>

        </div>

        <!-- 中央：報酬 -->
        <div class="chore-reward-area">
          <div class="reward-button">
            スタンプ <%= chore.stamp_reward %> 個
          </div>
        </div>

        <!-- 右：時間 -->
        <div class="chore-time-area">
          <span class="chore-date">
            <%= chore_date.execute_at.strftime("%-m/%-d") %>(<%= %w[日 月 火 水 木 金 土][chore_date.execute_at.wday] %>)
          </span><br>
          <span class="chore-time">
            <%= chore_date.execute_at.in_time_zone("Tokyo").strftime("%H:%M") %>
          </span>
        </div>

      </div>

    <% end %>

  <% else %>

  <p class="no-chore-message">過去やり残した家事はありません</p>
<% end %>

<% content_for :fixed_ui_left do %>
  <%= link_to "戻る", homes_index_path, class: "fixed-link" %>
<% end %>

<%= render "popup" %>