<% content_for :header_title do %>
  <div class="header-title-with-icon">
     <%= image_tag "キャプテン.png", class: "header-icon" %>
     成果
  </div>
<% end %>

<% content_for :main_class, "content-achievement" %>

<div class="member-tabs">
  <% @users.each do |user| %>

    <%= link_to achievements_path(user_id: user.id),
        class: "member-tab #{'active' if user == @user}" do %>

      <div class="member-tab-inner">

        <%= image_tag user.avatar_image_or_default,
            class: "user-avatar-achievement" %>

        <span class="member-name">
          <%= user.name %>
        </span>

      </div>

    <% end %>

  <% end %>
</div>


<div class="achievement-container">

  <div class="stamp-board">

    <% total_slots = 15 %>

    <% total_slots.times do |i| %>

      <% if @stamps[i] %>

        <div class="stamp active">
          <%= image_tag @stamps[i], class: "stamp-img" %>
        </div>

      <% else %>

        <div class="stamp empty">
          <span class="stamp-number"><%= i + 1 %></span>
        </div>

      <% end %>

    <% end %>

  </div>
</div>

<%= render "rewards/index" %>