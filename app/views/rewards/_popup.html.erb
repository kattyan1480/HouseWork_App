<div class="modal hidden js-reward-popup">
  <div class="modal-overlay"></div>

  <div class="modal-content">

    <!-- タイトル＋詳細 -->
    <div class="modal-title-area">
      <span class="modal-reward-title">
        <%= user.reward.title %>
      </span>

      <% if user == current_user %>
        <%= link_to edit_reward_path do %>
          <%= link_to image_tag("編集.png", class: "edit-icon"), edit_reward_path %>
        <% end %>
      <% end %>
    </div>

    <!-- コスト表示 -->
    <div class="reward-cost">
      <div class="reward-cost-label">
        獲得後に残るホールケーキ
    </div>

    <div class="reward-cost-value">
      <%= image_tag "holecake8.png", class: "reward-cake-icon" %>
      × <%= user.reward.cake_cost %>
    </div>

    <% if user == current_user %>
      <div class="modal-buttons">
        <div class="modal-buttons-complete">
          <%= button_to "獲得！",
              expend_reward_path,
              method: :post %>
        </div>
      </div>
    <% end %>

    <!-- 閉じる -->
    <div class="modal-close js-close-modal"></div>

  </div>
</div>