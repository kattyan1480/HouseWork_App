
@import "https://cdn.jsdelivr.net/npm/flatpickr/dist/flatpickr.min.css";

.flatpickr-calendar {
  transform: scale(1.15);
  transform-origin: top right;
}

html, body {
  height: 100%;
  margin: 0;
  overflow-y: scroll;     /* 縦スクロールは有効 */
  overflow-x: hidden;     /* 横は非表示 */
  scrollbar-width: none;  /* Firefox */
}

body {
  display: flex;
  flex-direction: column;
  font-family: 'Noto Sans JP', sans-serif;
  letter-spacing: 0.02em;
  -webkit-font-smoothing: antialiased;
  position: relative;
}

/* Chrome, Edge, Safari */
html::-webkit-scrollbar,
body::-webkit-scrollbar {
  display: none;
}

/* 背景画像 */
body::before {
  content: "";
  position: fixed;
  inset: 0;

  background-image: url("<%= asset_path 'marvel.jpg' %>");
  background-size: cover;
  background-position: center;

  opacity: 0.06; /* ← 薄さ（調整ポイント） */

  z-index: -1;
}

/* ===== ヘッダー ===== */
.app-header {
  background: linear-gradient(
    to bottom,
    #b71a31 0%,
    #9f1022 55%,
    #870d1c 100%
  );

  height: 50px;
  padding: 0 20px;

  display: flex;
  justify-content: space-between;
  align-items: center;
  box-sizing: border-box;

  border-bottom: 1px solid rgba(0,0,0,0.2);

  box-shadow:
    inset 0 1px 0 rgba(255,255,255,0.18),
    0 3px 6px rgba(0,0,0,0.18);

  flex-shrink: 0;

  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  z-index: 1000;
}

/* ===== フッター ===== */
.app-footer {
  background: linear-gradient(
    to bottom,
    #24304a 0%,
    #1b2a49 70%,
    #141f38 100%
  );

  position: fixed;
  bottom: 0;
  left: 0;
  width: 100%;
  height: 100px;

  padding: 0 10px;
  box-sizing: border-box;
  padding-bottom: calc(10px + env(safe-area-inset-bottom));

  display: flex;
  justify-content: space-between;
  align-items: center;

  border-top: 1px solid rgba(255,255,255,0.05);

  box-shadow:
    inset 0 1px 0 rgba(255,255,255,0.04),
    0 -2px 5px rgba(0,0,0,0.18);

  z-index: 1000;
}

.footer-item {
  color: #CCCCCC;
  text-decoration: none;
  display: flex;
  flex-direction: column;
  align-items: center;
  font-size: 14px;
}

.footer-icon {
  width: 30px;
  height: auto;
  padding-top: 2px;
}

.footer-icon-achievement {
  width: 13px;
  height: auto;
}

.header-center {
  position: absolute;
  left: 50%;
  transform: translateX(-50%);
  color: #ffffffda;
  font-size: 16px;
}

.avatar-upload {
  display: flex;
  flex-direction: column; /* 縦並び */
  gap: 6px; /* ボタンと文字の間の余白 */
}

.layout {
  display: flex;
  flex-direction: column;
  height: 100vh;
}

/* 画面全体の中央寄せ用 */
.content {
  flex: 1;
  display: flex;
  flex-direction: column;   /* 横中央 */
  align-items: center;
  width: 100%;
  overflow-y: auto;        /* 縦スクロール */
}

/* 画面全体の中央寄せ用 */
.content-achievement {
  flex: 1;
  display: flex;
  flex-direction: column;   /* 横中央 */
  align-items: center;
  width: 100%;
  overflow-y: auto;        /* 縦スクロール */
}

.content-inner {
  width: 100%;
  max-width: 420px;
  margin: 0 auto;
  padding-top: 50px;   /* ヘッダーの高さ */
  padding-bottom: 100px; /* フッターの高さ */
  display: flex;
  flex-direction: column;
  align-items: center;
}

/* 全体幅 */
.chore-list {
  width: 95%;
  display: flex;
  flex-direction: column;
  align-items: center;
}

/* 全体幅 */
.chore-list-history {
  width: 90%;
  display: flex;
  flex-direction: column;
}

/* =======================
   行デザイン
======================= */
.chore-row {
  display: flex;
  width: 100%;
  align-items: center;
  padding: 2px 0px;
  border: 1px solid #dddddd;
  cursor: pointer;
  box-shadow: 0 2px 6px rgba(0,0,0,0.05);
}

.chore-row:nth-of-type(odd) {
  background-color: #ffffff;
}

.chore-row:nth-of-type(even) {
  background-color: #f0f1f5;
}

/* 左側 */
.chore-main {
  flex: 1;
  display: flex;
  align-items: center;
  gap: 6px;
  margin-left: 4px;
}

.chore-icon {
  width: 50px;
  height: 50px;

  background-color: inherit;

  display: flex;
  align-items: center;
  justify-content: center;

  overflow: hidden;
}

.category-icon-home {
  width: 50px;              /* ← 少し小さく */
  height: 50px;
  object-fit: contain;      /* 画像比率を崩さない */

  margin: 0;                /* ← これが重要 */
}

/* テキストエリア */
.chore-text {
  display: flex;
  flex-direction: column;
  gap: 0px;
}

/* カテゴリバッジ */
.category-badge {
  display: inline-block;
  padding: 1px 6px;
  border-radius: 4px;
  font-size: 9px;
  color: #313131;
  font-weight: 600;
  width: fit-content;
}

/* 家事タイトル */
.chore-title {
  font-size: 16px;
  font-weight: 600;
  color: #383838;
}

/* =======================
   報酬
======================= */
.chore-reward-area {
  width: 32%;
  display: flex;
  align-items: center;
  justify-content: center;   /* ← 中央 */
}

.reward-button {
  background-color: #ffffff;
  border: 2px solid #444444;
  color: #383838;
  padding: 3px 5px;
  border-radius: 5px;
  font-size: 14px;
  font-weight: 600;
}

/* =======================
   時間
======================= */
.chore-time-area {
  width: 17%;
  text-align: center;

  font-size: 16px;
  font-weight: 700;
  letter-spacing: 1px;

  border-radius: 8px;
  color: #383838;
}

/* =======================
    アクション
======================= */
.chore-action-area {
  width: 140px;
  text-align: center;

  letter-spacing: 1px;

  border-radius: 8px;
  color: #383838;
}

/* =======================
    ユーザー
======================= */
.chore-user-area {
  width: 140px;
  text-align: center;

  border-radius: 8px;
  color: #383838;
}

/* 空状態カード */
.empty-chore-card {
  width:90%;
  background: #ffffff;
  border-radius: 4px;
  padding: 10px 20px;
  margin-top: 4px;
  text-align: center;
  box-shadow: 0 2px 6px rgba(0,0,0,0.05);
}

/* メッセージ */
.empty-message {
  font-size: 16px;
  color: #464646;
  margin-bottom: 10px;
  margin-top: 0px;
}

.empty-add-btn {
  display: block;
  background: #ffba3b;
  color: #333;
  text-decoration: none;
  padding: 8px 0;
  border-radius: 6px;
  font-weight: 500;
  font-size: 16px;
}

.edit-link {
  text-decoration: none;
}

.delete-link {
  text-decoration: none;
  position: absolute;
  top: 8px;
  left: 8px;
  z-index: 10;
}

/* フォーム＋エラーをまとめる箱 */
.form-wrapper {
  max-width: 100%;
  display: flex;
  flex-direction: column;
  gap: 16px;
}

/* ボタン */
.actions {
  display: flex;
  justify-content: center;
  margin-top: 20px;
}

/* フィールド */
.field {
  display: flex;
  flex-direction: column;
  padding-bottom: 16px;
}

.form-group.remember-me input[type="checkbox"] {
  transform: scale(1);
}

.form-group.remember-me {
  display: flex;
  flex-direction: row;
  align-items: center;
  gap: 12px;
  font-size: 16px;
}

.content.selectorjoin {
  display: flex;
  flex-direction: column; /* 縦並び */
  align-items: center;
  justify-content: flex-start;
  padding-top: 40px;
  gap: 16px;
}

.header-logo {
  display: flex;
  align-items: center;
}

.logo-image {
  height: 30px;
  width: auto;
  margin: 0px 0;   /* ← 上下余白 */
}

.hidden-calendar-input,
.hidden-calendar-input-reschedule {
  position: absolute;
  opacity: 0;
  pointer-events: none;
  height: 0;
  width: 0;
  font-size: 16px;
}

.stamp-icon {
  width: 30px;
  height: 30px;
  border-radius: 50%;
  aspect-ratio: 1 / 1;

  background: #fff;
  border: 1px solid #595a7e;   /* 少し太めに */

  box-shadow: 
    2px 2px 0 rgba(0, 0, 0, 0.1);  /* 押した感じの影 */

  display: flex;
  align-items: center;
  justify-content: center;

  transition: all 0.1s ease;
}

/* 標準の file input を非表示 */
.hidden-file {
  display: none;
}

/* ファイル選択ボタン */
#avatar_button {
  cursor: pointer;
  height: 40px;          /* ボタンの高さ */
  padding: 4px 10px;      /* ボタンの大きさ */
  font-size: 14px;          /* ボタン文字の大きさ */
  background-color: #ebe8e8; /* お好みの背景色 */
  border: 1px solid #555;
  border-radius: 6px;
  text-align: center;
}

/* ファイル名表示 */
#avatar_filename {
  font-size: 12px;          /* 文字の大きさ */
  color: #555;              /* 文字色 */
  min-height: 1em;          /* 選択されていないときも高さを確保 */
}

.user-avatar {
  width: 37px;
  height: 37px;
  border-radius: 50%; /* 丸くする */
  object-fit: cover;  /* はみ出さない */
  align-items: center; /* 縦中央揃え */
}

/* アイコンを横並びにする親要素 */
.chore-users-history {
  width: 89px;
  display: flex;
  justify-content: flex-end;   /* ← 右詰め */
  gap: 4px;                    /* アイコン間の隙間 */
  align-items: center;         /* 縦中央揃え */
  box-sizing: border-box;      /* ← paddingを幅に含める */
}

/* アイコン自体 */
.user-avatar-history {
  width: 50px;
  height: 50px;
  border-radius: 50%;
  object-fit: cover;
  line-height: 1;
  margin: 0;
  display: inline-block;
}

.avatar-preview {
  width: 80px;
  height: 80px;
  border-radius: 4px;
}

.header-profile {
  display: flex;
  align-items: center;
  gap: 6px;
}

.fixed-bottom-left {
  position: fixed;
  left: 10px;
  bottom: 110px;
  display: flex;
  gap: 12px;          /* ボタン間の隙間 */
  align-items: center;
  background-color: #e0e0e0;
}

.fixed-link {
  width: 90px;
  appearance: none;
  -webkit-appearance: none;

  display: inline-flex;
  align-items: center;
  justify-content: center;

  height: 50px;
  padding: 10px 10px;

  border-radius: 5px;

  font-size: 16px;
  font-weight: 500;
  font-family: inherit;
  line-height: 1;

  border: 1px solid #535353;   /* ← 枠線を明示 */
  color: #000000;

  text-decoration: none;
  box-sizing: border-box;

  box-shadow: 0 16px 13px -12px rgba(0, 0, 0, -19.44), 0 4px 25px 0px rgba(0, 0, 0, 0.12), 0 8px 10px -5px rgba(0, 0, 0, 0.2);
}

.hidden {
  display: none;
}

.modal {
  position: fixed;
  inset: 0;
  z-index: 1000;
}

.modal-overlay {
  position: absolute;
  inset: 0;
  background: rgba(0, 0, 0, 0.5);
}

/* モーダル全体の箱 */
.modal-content {
  position: relative;
  background: #ffffff;
  border: 1px solid #535353;   /* ← 枠線を明示 */
  width: 80%;
  max-width: 420px;
  padding-top: 25px;
  padding-bottom: 15px;
  margin: 150px auto;      /* 画面中央に配置 */
  border-radius: 8px;

  display: flex;
  flex-direction: column; /* 中身を縦並び */
  align-items: center;    /* 中身を横方向中央寄せ */
  text-align: center;
  color: #000000;
  font-weight: normal;
}

/* タイトル＋詳細 横並び */
.modal-title-area {
  display: flex;
  align-items: center;   /* 縦中央揃え */
  justify-content: center; /* 横中央揃え */
}

/* タイトル */
.modal-chore-title {
  font-size: 16px;
  font-weight: 500;
  font-weight: bold;
  margin: 0;      /* 余計なズレ防止 */
  line-height: 1;
  display: flex;
}

#complete-form {
  width: 100%;
  display: flex;
  align-items: center;
  justify-content: center;
  margin: 0;
}

.modal-members {
  display: flex;
  flex-direction: column;
  align-items: center;
  width: 82%;
}

.member-select {
  position: relative;
  width: 100%;
  padding-top: 20px;
}

/* 上のボックス */
.member-select-header {
  height: 40px;
  background: #d1d1d165;
  border: 1px solid #2e2e2e;
  padding: 3px 15px;
  cursor: pointer;
  display: flex;
  justify-content: space-between;
  align-items: center;
  font-size: 14px;
}

/* 下の一覧 */
.member-select-dropdown {
  position: absolute;
  top: 100%;
  box-sizing: border-box;
  width: 100%;
  height: 50px;
  background: #ffffff;
  border: 1px solid #a5a5a59a;
  box-shadow: 0 8px 20px rgba(0,0,0,0.1);
  overflow-y: auto;    /* ← スクロール */
  font-size: 14px;
  padding-top: 5px;
  text-align: left;
  display: none;
}

.member-checkbox {
  display: block;
  padding: 8px 8px;
  border-bottom: 1px solid #c7c7c7;
  cursor: pointer;
}

/* チェックボックスサイズ */
.member-checkbox input[type="checkbox"]{
  transform: scale(1.2);
  margin-right: 4px;
  margin-left: 8px;
}

.member-checkbox:last-child {
  border-bottom: none;
}

.member-checkbox:hover {
  background: #f5f5f5;
}

/* ボタン全体 */
.modal-buttons {
  display: flex;
  justify-content: center;
  width: 100%;
  margin-top: 20px;
}

.modal-buttons-gain {
  display: flex;
  justify-content: center;
  width: 100%;
}

.modal-buttons-complete {
  width: 100%;
  display: flex;
  flex-direction: column;
  align-items: center;
}

/* 1行目：完了ボタンを横いっぱいに */
.modal-buttons-gain button{
  width: 250px;
  height: 40px;
  padding: 3px 50px;
  border-radius: 4px;
  box-sizing: border-box; /* パディング込みで幅いっぱい */
  background-color: #57587e;
  color: #ffffff;
  margin-bottom: 4px;
  cursor: pointer;        /* 活性時はマウスカーソルを手に */
  transition: background 0.2s, color 0.2s; /* スムーズ切替 */
  font-size: 14px;
}

.modal-buttons-complete button {
  width: 100%;
  height: 40px;
  padding: 3px 50px;
  border-radius: 4px;
  box-sizing: border-box; /* パディング込みで幅いっぱい */
  background-color: #57587e;
  color: #ffffff;
  margin-bottom: 4px;
  cursor: pointer;        /* 活性時はマウスカーソルを手に */
  transition: background 0.2s, color 0.2s; /* スムーズ切替 */
  font-size: 14px;
}

/* ボタン全体 */
.modal-buttons-sub {
  display: flex;
  justify-content: center;
  width: 100%;
}

.modal-buttons-reschedule {
  display: flex;
  flex-direction: column;
  align-items: center;
  width: 100%;
  margin-top: 20px;
}

/* リスケボタン / 変更ボタンの共通スタイル */
.modal-buttons-reschedule button {
  width: 100%;
  height: 40px;
  margin: 0px;
  padding: 3px 100px;
  border-radius: 4px;
  box-sizing: border-box; /* パディング込みで幅いっぱい */
  border: 1px solid #000000;
  background: #e9e9e9f8;
  color: #000000;
  margin-bottom: 4px;
  cursor: pointer;        /* 活性時はマウスカーソルを手に */
  transition: background 0.2s, color 0.2s; /* スムーズ切替 */
  font-size: 14px;
}

/* ボタンが非活性（disabled）の時 */
.modal-buttons-reschedule button:disabled {
  background: #cccccc;    /* グレーっぽく */
  color: #666666;         /* 文字色も薄く */
  border: 1px solid #999999;
}

.hidden-calendar-input-reschedule {
  position: absolute;
  opacity: 0;
  pointer-events: none;
  height: 0;
  width: 0;
}

/* =========================
   削除アイコン（左上）
========================= */
#modal-delete {
  position: absolute;
  top: 12px;
  left: 12px;
  z-index: 10;
}

/* ==============================
   reward専用
================================ */

/* タイトル（choreと同じ見た目） */
.modal-reward-title {
  font-size: 16px;
  font-weight: 500;
  font-weight: bold;
  margin: 0;
  padding-bottom: 8px;
}

/* コスト表示 */
.reward-cost {
  display: flex;
  flex-direction: column;  /* ← 縦並びにする */
  align-items: center;
  gap: 4px;
  font-size: 16px;
  padding-top: 8px;
}

.reward-cost-value {
  display: flex;
  align-items: center;
  gap: 4px;
  padding-bottom: 15px;
}

.reward-stamp-icon {
  width: 40px;
  height: 40px;
  display: block;
}

/* =========================
   タブ全体
========================= */
.member-tabs {
  display: flex;
  flex-wrap: nowrap;          /* 折り返さない */
  overflow-x: auto;           /* 横スクロール */
  overflow-y: hidden;
  white-space: nowrap;
  border-bottom: 1px solid #ddd;
  background: #ffffff;
  width: 100%;
  height: 40px;
  box-sizing: border-box;
  -webkit-overflow-scrolling: touch; /* スマホでなめらかに */
}

/* スクロールバーを消したい場合 */
.member-tabs::-webkit-scrollbar {
  display: none;
}
.member-tabs {
  scrollbar-width: none;
}

/* =========================
   タブ
========================= */
.member-tab {
  flex: 0 0 auto;             /* 縮まない・自動幅 */
  text-decoration: none;
  padding: 5px 14px;
  position: relative;
  display: flex;
  justify-content: center;
  align-items: center;
  box-sizing: border-box;
}

/* 中身（アイコン＋名前） */
.member-tab-inner {
  display: flex;
  align-items: center;
  gap: 8px;
  white-space: nowrap;        /* 名前を改行させない */
}

/* 名前 */
.member-name {
  font-size: 16px;
  color: #111111;
  font-weight: 500;
  white-space: nowrap;
}

/* アクティブタブ */
.member-tab.active::after {
  content: "";
  position: absolute;
  left: 0;
  bottom: -1px;
  width: 100%;
  height: 4px;
  background: #f3a13b;
}

/* アバター */
.user-avatar-achievement {
  width: 30px;
  height: 30px;
  border-radius: 50%;
  object-fit: cover;
  flex-shrink: 0;
}

/* =========================
   ステータスエリア
========================= */
.status-bar{
  width:95%;
  display:flex;
  justify-content:center;
  margin-top:20px;
}

/* カード */
.status-item{
  width:100%;
  height:100%;
  display:flex;
  justify-content:space-around;
  align-items:center;

  padding:5px 5px;

  background:rgba(255,255,255,0.95);
  border-radius:4px;

  box-shadow: 0 2px 6px rgba(0,0,0,0.05);
}

/* 各ブロック */
.status-block{
  display:flex;
  flex-direction:column;
  align-items:center;
  gap:5px;
}

/* スタンプ表示部分 */
.status-stamp{
  display:flex;
  align-items:center;
  gap:6px;
  font-size:18px;
  font-weight:600;
}

/* 区切り線 */
.status-divider{
  width:1px;
  height:60px;
  background:#ddd;
}

/* アイコン */
.status-icon{
  width:35px;
  height:35px;
}

/* ラベル */
.status-label{
  font-size:14px;
  color:#666;
}

.multiplication{
  font-size:14px;
}

.achievement-container {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 4px;
  width: 100%;
}

/* =========================
   スタンプ帳台紙
========================= */
.stamp-board{
  width:100%;
  height:280px;

  background-image:url("<%= asset_path 'stampsheet.jpg' %>");
  background-size:100% auto;
  background-repeat:no-repeat;
  background-position:top center;

  position:relative;

  display:grid;
  grid-template-columns:repeat(5,65px);
  justify-content:center;
  align-content:start;
  gap:10px;
  padding-top:30px;
}

/* 薄い白レイヤー */
.stamp-board::before{
  width:95%;
  height:72%;
  content:"";
  position:absolute;
  inset:0;
  margin-top:19px;

  left:50%;
  transform:translateX(-50%);  /* 横中央 */
  background:rgba(255,255,255,0.6);  /* 白の透明 */

  border-radius:12px;
  z-index:1;
}

/* スタンプ共通 */
.stamp{
  width:60px;
  height:60px;
  border-radius:50%;
  display:flex;
  align-items:center;
  justify-content:center;
  position:relative;
  z-index:2;
}

/* 取得済み */
.stamp.active{
  background: #ffffff;
  border:1px solid #dadada;
  box-shadow:
  0 3px 6px rgba(0,0,0,0.2);
}

/* スタンプ画像 */
.stamp-img{
  width:100%;
}

/* 未取得 */
.stamp.empty{
  background: rgba(199, 199, 199, 0.35);
}

.stamp-number{
  font-size:14px;
  color:#ffffff;
  font-weight:400;
  font-family: "Noto Sans JP", "Yu Gothic", sans-serif;
}

.reward-container {
  display: flex;
  width: 100%;
  flex-direction: column;
  align-items: center;
  gap: 4px;

  margin-top: 10px;

  position: relative;
  cursor: pointer;
}

.reward-box {
  width: 95%;
  height: 88px;
  padding: 4px 20px;

  box-sizing: border-box;
  display: flex;
  justify-content: space-between;
  align-items: center;

  color: #ffffff;
  background-size: cover;
  background-position: center;

  border-radius: 12px;
  border: 1px solid #e0e0e0;
  box-shadow: 0 6px 18px rgba(0, 0, 0, 0.08);
}

.reward-bg-0 {
    background-image: url("<%= asset_path 'ご褒美背景1.jpg' %>"); /* 画像パス */
}

.reward-bg-1 {
    background-image: url("<%= asset_path 'ご褒美背景2.jpg' %>"); /* 画像パス */
}

.reward-bg-2 {
    background-image: url("<%= asset_path 'ご褒美背景3.jpg' %>"); /* 画像パス */
}

/* 左側 */
.reward-left {
  display: flex;
  flex-direction: column;
  justify-content: space-between;
}

.reward-title {
  font-size: 14px;
  font-weight: 700;
  padding-bottom: 2px;
}

.reward-need {
  font-size: 14px;
  color: #ffffff;
}

/* 右側 */
.reward-right {
  display: flex;
  align-items: center;
}

/* 丸表示 */
.reward-circle {
  width: 60px;
  height: 60px;
  border-radius: 50%;

  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;

  font-size: 14px;
  font-weight: 600;
  text-align: center;
}

/* 未達成 */
.reward-circle.ng {
  background: #fff3f3;
  color: #e05353;
  border: 2px solid #d32f2f;
  font-size: 14px;
  font-weight: 600;
}

/* 達成 */
.reward-circle.ok {
  background: #f0ffe4;
  color: #3b3a3a;
  border: 2px solid #6f9165;
}

/* 「達成まで」だけ黒文字 */
.reward-circle.ng .reward-label {
  color: #000000;
  font-size: 12px;
  font-weight: normal;
}

.stamp-count {
  display: inline-flex;
  align-items: center;
  gap: 4px;
  font-weight: bold;
}

.mini-stamp-reward {
  width:  30px;
  height: 30px;
}

.detail-edit-link {
  text-decoration: none;
}

.detail-icon,
.edit-icon,
.delete-icon {
  width: 20px;
  height: auto;
  cursor: pointer;
}

.edit-icon-chore-index {
  width: 20px;
  height: auto;
  cursor: pointer;
  margin-right: 14px;
}

.detail-icon-home {
  width: 20px;
  height: 20px;
  display: block;
  padding-top: 4px;
}

.edit-icon-reward {
  width: 20px;
  height: 20px;
  display: block;
  padding-bottom: 5px;
}

.stamp-wrapper {
  position: relative;
  display: inline-block;
}

.stamp-icon.whole {
  width: 200px;
  padding-left: 15px;
}

.stamp-info {
  position: absolute;
  background: #fff;
  width: 90px;
  height: 90px;
  border-radius: 50%;
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  font-size: 13px;
  box-shadow: 0 4px 10px rgba(0,0,0,0.15);
  text-align: center;
}

.stamp-info.top-right {
  top: 20px;
  right: 40px;
}

.stamp-info .number {
  font-weight: bold;
  color: #e74c3c; /* 赤系でケーキ感 */
}

.stamp-count-plain {
  position: absolute;
  bottom: 10px;
  right: 25px;
  display: flex;
  flex-direction: column; /* 縦並び */
  align-items: flex-end;  /* 右寄せ */
  gap: 4px;
  font-size: 14px;
  color: #333;
}

.stamp-count-label {
  font-size: 13px;
}

.stamp-count-body {
  display: flex;
  align-items: center;
  gap: 6px;
}

.mini-stamp {
  width: 28px;
  height: auto;
}

.stamp-count-body .count {
  font-weight: bold;
}

.form-wrapper-chore {
  width: 100%;
  display: flex;
  justify-content: center;
  align-items: center;
  padding: 30px 0;
}

.form-card {
  background: #ffffff;
  width: 85%;                /* 少し広げる */
  padding: 10px 20px;
  border-radius: 4px;         /* 角丸を少し大きく */
  border: 1px solid #dcdfe3;
  box-shadow: 0 8px 24px rgba(0, 0, 0, 0.06); /* うっすら影 */
}

.form-title {
  font-size: 16px;
  font-weight: 600;
  margin-bottom: 28px;
  padding-bottom: 12px;
  border-bottom: 1px solid #e5e7eb;
  color: #333;
}

/* グループ間余白を増やす */
.form-group {
  margin-bottom: 14px;
}

.form-label {
  display: inline-block;
  font-size: 16px;
  font-weight: 600;
  margin-bottom: 4px;
  color: #6b7280; /* 少し薄く */
  letter-spacing: 0.05em;
}

.label-row {
  display: flex;
  align-items: center;
  gap: 8px;
}

.required {
  display: inline-block;
  padding: 2px 4px;
  font-size: 12px;
  color: #c62828;
  background: #fdecea;
  border-radius: 4px;
  margin-bottom: 6px;
}

.form-input,
.form-textarea {
  width: 100%;
  padding: 9px 10px;
  border-radius: 4px;
  border: 1px solid #cfd3d8;
  background: #ffffff;
  font-size: 16px;
  box-sizing: border-box;   /* ← これを追加 */
}

.form-input:focus,
.form-textarea:focus {
  outline: none;
  border-color: #5b7dbd;
}

.form-input::placeholder,
.form-textarea::placeholder {
  color: #9aa0a6;
}

.time-input-wrapper {
  position: relative;
  width: 100%;
}

.time-input {
  height: 40px;
  font-size: 16px;
  padding: 0px 0px;

  border-radius: 4px;
  border: 1px solid #ccc;
  box-sizing: border-box;
}

.time-icon {
  position: absolute;
  left: 12px;
  top: 50%;
  transform: translateY(-50%);
  font-size: 16px;
  pointer-events: none;
  color: #888;
}

/* show専用の表示フィールド */
/* 表示フィールド */
.display-field {
  background-color: #f9fafb;
  padding: 12px 14px;
  border-radius: 8px;
  border: 1px solid #e5e7eb;
  font-size: 16px;
  color: #111827;
  pointer-events: none;
}

/* textarea表示用 */
.form-textarea.display-field {
  min-height: 110px;
  line-height: 1.6;
}

.calendar-btn {
  width: 100%;
  padding: 9px 10px;
  border-radius: 4px;
  border: 1px solid #cfd3d8;
  background: #ffffff;
  font-size: 16px;
  color: #333;
  cursor: pointer;
  text-align: left;
}

.calendar-btn:hover {
  background: #f3f4f6;
}

.form-actions {
  margin-top: 20px;
  display: flex;
  justify-content: space-between; /* ← 左右に分ける */
  align-items: center;
}

.form-actions-show {
  display: flex;
  justify-content: flex-end;  /* ← これに変更 */
}

.submit-btn {
  padding: 12px 34px;  /* 横幅はpaddingで調整 */
  border-radius: 6px;
  border: none;
  background: #f3b769;
  color: #313131;
  font-size: 16px;
  font-weight: 600;
  cursor: pointer;
}

.submit-btn:hover {
  opacity: 0.9;
}

.back-link {
  display: inline-block;
  padding: 10px 14px;
  border-radius: 6px;
  border: 1px solid #d1d5db;
  background: #ffffff;
  font-size: 16px;
  color: #374151;
  text-decoration: none;
  transition: all 0.2s ease;
}

.back-link:hover {
  background: #f3f4f6;
}

/* 外側の箱 */
.home-links-box {
  width: 85%;
  display: flex;
  flex-direction: column;
  gap: 10px;             /* ← ボタン間の隙間 */
  margin-top: 30px;
  margin-bottom: 130px;
}

/* 各ボタン */
.home-link-item {
  display: flex;
  align-items: center;
  justify-content: center;

  padding: 10px;

  /* グラデーションで立体感 */
  background: #ffba3b;

  border-radius: 6px;

  text-decoration: none;
  color: #333;

  box-shadow:
    inset 0 1px 0 rgba(255,255,255,0.15), /* 上の光 */
    0 2px 4px rgba(0,0,0,0.18);           /* 下の影 */

  transition: all 0.15s ease;
}

/* テキスト */
.link-text {
  font-size: 16px;
  font-weight: 500;
  margin-right: 10px;
}

.chore-create-btn {
  position: fixed;      /* 画面固定 */
  right: 10px;          /* 右から */
  bottom: 110px;         /* 下から */

  width: 50px;
  height: 50px;
  border-radius: 50%;

  background-color: #ffba3b;
  color: white;

  display: flex;
  align-items: center;
  justify-content: center;

  font-size: 28px;
  font-weight: bold;
  text-decoration: none;

  box-shadow: 0 4px 10px rgba(0,0,0,0.2);
  transition: transform 0.2s, box-shadow 0.2s;
}

.field-error-message {
  color: #e53935;
  font-size: 14px;
  margin-top: 6px;
}

.input-error {
  border: 2px solid #e53935;
  background-color: #fff5f5;
}

.calendar-btn.input-error {
  border: 2px solid #e53935 !important;
  background-color: #fff5f5;
}

.devise-links-vertical {
  margin-top: 24px;
  display: flex;
  flex-direction: column;
  gap: 12px;
}

.devise-link-item {
  display: block;
  text-align: center;
  padding: 10px;
  border-radius: 8px;
  text-decoration: none;
  font-size: 16px;
  background-color: #d6d6d6;
  color: #333;
  transition: 0.2s ease;
}

.omniauth-btn {
  border: none;
  cursor: pointer;
}

/* 共通 */
.flash-message {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  padding: 14px 0;
  text-align: center;
  font-weight: bold;
  z-index: 9999;

  transform: translateY(-100%);
  opacity: 0;
  transition: all 0.5s ease;

  font-size: 16px;
}

/* 表示状態 */
.flash-show { 
  transform: translateY(0); 
  opacity: 1; }

/* 成功 */
.flash-notice {
  background-color: #cde6d3;
  color: #2c2c2c;
}

/* 失敗 */
.flash-alert {
  background-color: #f8d7da;
  color: #a02632;
}

.yesterday-chore-schedule,
.today-chore-schedule {
  display: flex;         
  align-items: center;
  gap: 6px;
  font-size: 16px;
  margin-bottom: 6px;
  align-self: flex-start; 
  font-weight: 500;
  color: #333;
  margin-top: 30px;
}

.checklist-icon {
  width: 24px;
  height: 24px;
  object-fit: contain;   /* ← 形を崩さない */
  flex-shrink: 0;
}

.no-chore-message {
  text-align: center;
  margin-top: 20px;
  font-size: 16px;
  color: #464646;
}

.form-description {
  font-size: 14px;
  color: #444444;
  margin-top: 4px;
  margin-left: 17px;
}

.font-weight-bold {
  font-weight: 800;
}

.selector-buttons {
  display: flex;
  flex-direction: column;
  gap: 16px;
}

.secondary-btn {
  background-color: #d6d6d6;   /* 少し淡い色 */
  color: #333;
}

.secondary-btn:hover {
  opacity: 0.9;
}

.selection-title-user {
  display: flex;
  align-items: center;   /* 縦中央揃え */
  gap: 12px;
  margin-bottom: 14px;
}

.selection-title {
  display: flex;
  align-items: center;   /* 縦中央揃え */
  gap: 12px;
  margin-bottom: 14px;
  margin-top: 40px;
}

.form-selection-title {
  font-size: 16px;
  font-weight: bold;
  color: #333;
  margin: 0;   /* ← 余計なズレ防止 */
}

.file-name {
  margin-top: 8px;
  font-size: 16px;
  color: #666;
}

/* =========================
   サイドメニュー本体
========================= */
.side-menu {
  position: fixed;
  top: 0;
  left: -290px;
  width: 280px;
  height: 100%;
  background: #1e1f23;   /* 暗め背景 */
  box-shadow: 4px 0 15px rgba(0,0,0,0.4);
  transition: left 0.3s ease;
  z-index: 1001;
  padding-top: 60px;   /* 上に余白 */
}

/* 開いた状態 */
.side-menu.active {
  left: 0;
}

/* =========================
   メニューリンク
========================= */
.side-menu a {
  display: flex;              /* ← 横並びにする */
  align-items: center;
  padding: 16px 22px;
  text-decoration: none;
  color: #dcdcdc;
  font-size: 16px;
  font-weight: 500;
  transition: 0.2s;
  border-radius: 8px;
  margin: 6px 12px;
}

/* アイコン部分 */
.menu-icon {
  margin-right: 30px;
  width: 26px;               /* 揃える */
  text-align: center;
}

.menu-avatar {
  width: 40px;
  height: 40px;
  border-radius: 50%;
  object-fit: cover;
}

.menu-avatar-square {
  width: 40px;
  height: 40px;
  object-fit: cover;
}

/* hover時 */
.side-menu a:hover {
  background: rgba(255, 255, 255, 0.06);
  color: #ffffff;
}

/* ログアウトを少し目立たせる */
.side-menu a:last-child {
  margin-top: 20px;
  border-top: 1px solid rgba(255,255,255,0.08);
  padding-top: 20px;
}

/* =========================
   オーバーレイ
========================= */
.overlay {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background: rgba(0, 0, 0, 0.55);
  backdrop-filter: blur(2px);
  display: none;
  z-index: 1000;
}

.overlay.active {
  display: block;
}

/* =========================
   ハンバーガー強調
========================= */
.hamburger {
  cursor: pointer;
  margin-bottom: 10px;
}

.hamburger .footer-icon {
  font-size: 30px;
}

.category-wrapper {
  padding: 20px;
  width: 100%;
  max-width: 420px;
  margin: 0 auto;
}

.category-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-top: 20px;
  width: 100%;       /* 必要に応じて */
  margin-left: auto;  /* 右寄せ */
}

.category-add-btn {
  background: #f3b769;
  color: #333;
  padding: 0px 20px;
  height: 40px;
  border-radius: 8px;
  text-decoration: none;
  display: flex;
  align-items: center;
  font-size: 16px;
  font-weight: 600;
}

.category-grid {
  display: grid;
  grid-template-columns: repeat(3, 100px);
  gap: 20px;
}

.category-card {
  width: 100%;
  height: 120px;
  position: relative;
  background: #ffffff;
  border-radius: 16px;
  padding: 30px 5px;
  text-align: center;
  text-decoration: none;
  color: #333;
  box-shadow: 0 4px 10px rgba(0,0,0,0.08);
  transition: 0.2s ease;
}

.category-icon {
  width: 80px;
  height: 80px;
  margin: 0 auto 12px;

  background: #fff;            /* 背景を白に */
  
  display: flex;
  align-items: center;
  justify-content: center;

  overflow: hidden;            /* はみ出し防止 */
}

.category-icon img {
  max-width: 95%;     /* ← 少し小さめでスタンプ感 */
  max-height: 95%;
  object-fit: contain; /* 切らずに収める */
}

.category-name {
  font-size: 16px;
  font-weight: 600;
  padding: 4px 8px;
  border-radius: 8px;
  display: inline-block;
}

.category-empty {
  text-align: center;
  margin-top: 40px;
  color: #777;
  font-size: 16px;
}

.card-main-link {
  display: block;
  text-decoration: none;
  color: inherit;
}

.color-options {
  display: flex;
  gap: 12px;
  flex-wrap: wrap;
  margin-top: 8px;
}

.color-choice {
  position: relative;
  cursor: pointer;
}

.color-choice input {
  position: absolute;
  opacity: 0;
}

.color-circle {
  width: 28px;
  height: 28px;
  border-radius: 50%;
  border: 2px solid #ddd;
  display: inline-block;
  transition: 0.1s;
}

/* 選択時 */
.color-choice input:checked + .color-circle {
  border: 2px solid #7389c5;
  transform: scale(1.1);
}

.header-title-with-icon {
  display: flex;
  align-items: center;
  gap: 6px;
}

.header-icon {
  width: 30px;
  height: 30px;
}
