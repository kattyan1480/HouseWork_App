
@import "https://cdn.jsdelivr.net/npm/flatpickr/dist/flatpickr.min.css";

html, body {
  height: 100%;
  margin: 0;
}

body {
  flex-direction: column;
  background-color: #f5fafff8;
  overflow: hidden;
  font-family: 'Noto Sans JP', sans-serif;
  font-weight: 400;
  letter-spacing: 0.02em;
  -webkit-font-smoothing: antialiased;
  position: relative;
}

/* ===== ヘッダー ===== */
.app-header {
  background: linear-gradient(
    to bottom,
    #24304a 0%,
    #1b2a49 70%,
    #141f38 100%
  );

  height: 56px;
  padding: 0 20px;

  display: flex;
  justify-content: space-between;
  align-items: center;

  border-bottom: 1px solid rgba(0,0,0,0.2);

  box-shadow:
    inset 0 1px 0 rgba(255,255,255,0.18),
    0 3px 6px rgba(0,0,0,0.18);

  flex-shrink: 0;
}

/* ===== フッター ===== */
.app-footer {
  background: linear-gradient(
    to bottom,
    #24304a 0%,
    #1b2a49 70%,
    #141f38 100%
  );

  height: 48px;
  padding: 0 40px;

  display: flex;
  justify-content: space-between;
  align-items: center;

  border-top: 1px solid rgba(255,255,255,0.05);

  box-shadow:
    inset 0 1px 0 rgba(255,255,255,0.04),
    0 -2px 5px rgba(0,0,0,0.18);

  flex-shrink: 0;
}

.footer-item {
  color: #CCCCCC;
  text-decoration: none;
  display: flex;
  flex-direction: column;
  align-items: center;
  font-size: 12px;
}

.footer-icon {
  width: 18px;
  height: auto;
  padding-top: 2px;
}

.footer-icon-achievement {
  width: 13px;
  height: auto;
}

.header-center {
  position: absolute;
  left: 50%;
  transform: translateX(-50%);
  color: #ffffffda;
}

.avatar-upload {
  display: flex;
  flex-direction: column; /* 縦並び */
  gap: 6px; /* ボタンと文字の間の余白 */
}

.layout {
  display: flex;
  flex-direction: column;
  height: 100vh;
}

/* 画面全体の中央寄せ用 */
.content {
  flex: 1;
  display: flex;
  flex-direction: column;   /* 横中央 */
  padding-top: 20px;
  align-items: center;
  width: 100%;
  overflow-y: auto;        /* 縦スクロール */
}

.content-inner {
  width: 100%;
  max-width: 1200px;
  margin: 0 auto;
  padding-bottom: 50px;
  display: flex;
  flex-direction: column;
  align-items: center;
}

/* 全体幅 */
.chore-list {
  width: 42%;
  display: flex;
  flex-direction: column;
}

/* 全体幅 */
.chore-list-history {
  width: 50%;
  display: flex;
  flex-direction: column;
}

/* =======================
   ヘッダー
======================= */
.chore-row-header {
  display: flex;
  height: 34px;
  border: 1px solid #444444;   /* 外枠 */
  border-bottom: none;
}

.chore-row-header > div {
  display: flex;
  align-items: center;
  justify-content: center;
  background-color: #444444;
  color: white;
  font-size: 14px;
  font-weight: 600;
  /* 区切り線 */
  border-right: 2px solid #ffffff;
}

/* 一番右だけ線を消す */
.chore-row-header > div:last-child {
  border-right: none;
}

.chore-title-header-homes { flex: 1; }
.chore-reward-header-homes   { width: 140px; }
.chore-date-header-homes     { width: 80px; }

.chore-title-header-chores-index { flex: 1; }
.chore-reward-header-chores-index   { width: 140px; }
.chore-action-header-chores-index     { width: 80px; }

.chore-title-header-chores-history { flex: 1; }
.chore-reward-header-chores-history   { width: 140px; }
.chore-date-header-chores-history     { width: 80px; }
.chore-user-header-chores-history     { width: 100px; }

/* =======================
   行デザイン
======================= */
.chore-row {
  display: flex;
  align-items: center;
  padding: 4px 0px;
  border: 1px solid #dddddd;
  border-top: none;
  cursor: pointer;
}

.chore-row:nth-of-type(odd) {
  background-color: #f0f1f5;
}

.chore-row:nth-of-type(even) {
  background-color: #ffffff;
}

/* 左側 */
.chore-main {
  flex: 1;
  display: flex;
  align-items: center;
  gap: 14px;
  margin-left: 12px;
}

.chore-icon {
  width: 39px;
  height: 39px;
  border-radius: 50%;

  background-color: inherit;
  border: 1px solid rgba(0,0,0,0.05); /* うっすら縁 */

  display: flex;
  align-items: center;
  justify-content: center;

  overflow: hidden;
}

.category-icon-home {
  width: 38px;              /* ← 少し小さく */
  height: 38px;
  object-fit: contain;      /* 画像比率を崩さない */

  margin: 0;                /* ← これが重要 */
}

/* テキストエリア */
.chore-text {
  display: flex;
  flex-direction: column;
}

/* カテゴリバッジ */
.category-badge {
  display: inline-block;
  padding: 1px 6px;
  border-radius: 6px;
  font-size: 10px;
  color: #313131;
  font-weight: 600;
  width: fit-content;
}

/* 家事タイトル */
.chore-title {
  font-size: 15px;
  font-weight: 700;
  color: #383838;
}

/* =======================
   報酬
======================= */
.chore-reward-area {
  width: 140px;
  display: flex;
  align-items: center;
  justify-content: center;   /* ← 中央 */
}

.reward-button {
  background-color: #ffffff;
  border: 2px solid #444444;
  color: #383838;
  padding: 5px 10px;
  border-radius: 5px;
  font-size: 12px;
  font-weight: 600;
}

/* =======================
   時間
======================= */
.chore-time-area {
  width: 80px;
  text-align: center;

  font-size: 14px;
  font-weight: 700;
  letter-spacing: 1px;

  border-radius: 8px;
  color: #383838;
}

/* =======================
    アクション
======================= */
.chore-action-area {
  width: 80px;
  text-align: center;

  font-size: 14px;
  font-weight: 700;
  letter-spacing: 1px;

  border-radius: 8px;
  color: #383838;
}

/* =======================
    アクション
======================= */
.chore-user-area {
  width: 100px;
  text-align: center;

  border-radius: 8px;
  color: #383838;
}

/* =======================
   空状態
======================= */
.empty-chore-card {
  border: 1px solid #ddd;
  padding: 30px;
  text-align: center;
}

.empty-add-btn {
  display: block;
  background: #f3b769;
  color: #333;
  text-decoration: none;
  padding: 8px 0;
  border-radius: 6px;
  font-weight: 500;
}

.edit-link {
  text-decoration: none;
}

.delete-link {
  text-decoration: none;
}

/* フォーム＋エラーをまとめる箱 */
.form-wrapper {
  width: 360px;          /* 基準幅（重要） */
  max-width: 90%;
  display: flex;
  flex-direction: column;
  gap: 16px;
}

/* ボタン */
.actions {
  display: flex;
  justify-content: center;
  margin-top: 20px;
}

/* フィールド */
.field {
  display: flex;
  flex-direction: column;
  padding-bottom: 16px;
}

.field input:not([type="checkbox"]) {
  width: 100%;
  box-sizing: border-box;
}

.field.remember-me {
  display: flex;
  flex-direction: row;
  align-items: center;
  gap: 6px;
}

.content.selectorjoin {
  display: flex;
  flex-direction: column; /* 縦並び */
  align-items: center;
  justify-content: flex-start;
  padding-top: 40px;
  gap: 16px;
}

.header-logo {
  display: flex;
  align-items: center;
}

.logo-image {
  height: 30px;
  width: auto;
  margin: 0px 0;   /* ← 上下余白 */
}

.hidden-calendar-input {
  position: absolute;
  opacity: 0;
  pointer-events: none;
  height: 0;
  width: 0;
}

.stamp-icon {
  width: 30px;
  height: 30px;
  border-radius: 50%;
  aspect-ratio: 1 / 1;

  background: #fff;
  border: 1px solid #595a7e;   /* 少し太めに */

  box-shadow: 
    2px 2px 0 rgba(0, 0, 0, 0.1);  /* 押した感じの影 */

  display: flex;
  align-items: center;
  justify-content: center;

  transition: all 0.1s ease;
}

/* 標準の file input を非表示 */
.hidden-file {
  display: none;
}

/* ファイル選択ボタン */
#avatar_button {
  cursor: pointer;
  padding: 4px 10px;      /* ボタンの大きさ */
  font-size: 12px;          /* ボタン文字の大きさ */
  background-color: #ebe8e8; /* お好みの背景色 */
  border: 1px solid #555;
  border-radius: 6px;
  text-align: center;
}

/* ファイル名表示 */
#avatar_filename {
  font-size: 12px;          /* 文字の大きさ */
  color: #555;              /* 文字色 */
  min-height: 1em;          /* 選択されていないときも高さを確保 */
}

.user-avatar {
  width: 37px;
  height: 37px;
  border-radius: 50%; /* 丸くする */
  object-fit: cover;  /* はみ出さない */
  align-items: center; /* 縦中央揃え */
}

/* アイコンを横並びにする親要素 */
.chore-users-history {
  width: 89px;
  display: flex;
  justify-content: flex-end;   /* ← 右詰め */
  gap: 4px;                    /* アイコン間の隙間 */
  align-items: center;         /* 縦中央揃え */
  box-sizing: border-box;      /* ← paddingを幅に含める */
}

/* アイコン自体 */
.user-avatar-history {
  width: 34px;
  height: 34px;
  border-radius: 50%;
  object-fit: cover;
  line-height: 1;
  margin: 0;
  display: inline-block;
}

.avatar-preview {
  width: 80px;
  height: 80px;
  border-radius: 4px;
}

.header-profile {
  display: flex;
  align-items: center;
  gap: 6px;
}

.fixed-bottom-right {
  position: fixed;
  right: 40px;
  bottom: 80px;
  display: flex;
  gap: 12px;          /* ボタン間の隙間 */
  align-items: center;
}

.fixed-bottom-left {
  position: fixed;
  left: 40px;
  bottom: 80px;
  display: flex;
  gap: 12px;          /* ボタン間の隙間 */
  align-items: center;
  background-color: #e0e0e0;
}

.fixed-link {
  width: 110px;
  appearance: none;
  -webkit-appearance: none;

  display: inline-flex;
  align-items: center;
  justify-content: center;

  height: 50px;
  padding: 0 10px;

  border-radius: 5px;

  font-size: 15px;
  font-weight: 500;
  font-family: inherit;
  line-height: 1;

  border: 1px solid #535353;   /* ← 枠線を明示 */
  color: #000000;

  text-decoration: none;
  box-sizing: border-box;

  box-shadow: 0 16px 13px -12px rgba(0, 0, 0, -19.44), 0 4px 25px 0px rgba(0, 0, 0, 0.12), 0 8px 10px -5px rgba(0, 0, 0, 0.2);
}

.hidden {
  display: none;
}

.modal {
  position: fixed;
  inset: 0;
  z-index: 1000;
}

.modal-overlay {
  position: absolute;
  inset: 0;
  background: rgba(0, 0, 0, 0.5);
}

/* モーダル全体の箱 */
.modal-content {
  position: relative;
  background: #ffffff;
  border: 1px solid #535353;   /* ← 枠線を明示 */
  width: 230px;
  padding-top: 25px;
  padding-bottom: 5px;
  margin: 100px auto;      /* 画面中央に配置 */
  border-radius: 8px;

  display: flex;
  flex-direction: column; /* 中身を縦並び */
  align-items: center;    /* 中身を横方向中央寄せ */
  text-align: center;
  color: #000000;
  font-weight: normal;
}

/* タイトル＋詳細 横並び */
.modal-title-area {
  display: flex;
  align-items: center;   /* 縦中央揃え */
  justify-content: center; /* 横中央揃え */
}

/* タイトル */
.modal-chore-title {
  font-size: 15px;
  font-weight: 500;
  font-weight: bold;
  margin: 0;      /* 余計なズレ防止 */
  line-height: 1;
  display: flex;
}

#complete-form {
  margin: 0;
}

.member-select {
  position: relative;
  width: 170px;
  padding-top: 20px;
}

/* 上のボックス */
.member-select-header {
  background: #d1d1d165;
  border: 1px solid #2e2e2e;
  padding: 5px 7px;
  cursor: pointer;
  display: flex;
  justify-content: space-between;
  align-items: center;
}

/* 下の一覧 */
.member-select-dropdown {
  position: absolute;
  top: 100%;
  box-sizing: border-box;
  width: 100%;
  background: #ffffff;
  border: 1px solid #a5a5a59a;
  box-shadow: 0 8px 20px rgba(0,0,0,0.1);
  max-height: 180px;   /* ← 高さ固定 */
  overflow-y: auto;    /* ← スクロール */
  display: none;
}

.member-checkbox {
  display: block;
  padding: 8px 8px;
  border-bottom: 1px solid #c7c7c7;
  cursor: pointer;
}

.member-checkbox:last-child {
  border-bottom: none;
}

.member-checkbox:hover {
  background: #f5f5f5;
}

/* ボタン全体 */
.modal-buttons {
  display: flex;
  flex-direction: column; 
  align-items: stretch;   /* ← 中身を横幅いっぱいに伸ばす */
  width: 100%;         /* モーダル幅いっぱい */
  padding-top: 20px;
  padding-bottom: 4px;
}

.modal-buttons-gain {
  display: flex;
  flex-direction: column; 
  align-items: stretch;   /* ← 中身を横幅いっぱいに伸ばす */
  width: 100%;         /* モーダル幅いっぱい */
}

/* 1行目：完了ボタンを横いっぱいに */
.modal-buttons-complete button {
  width: 100%;            /* 親に合わせて横いっぱい */
  margin: 0px;
  padding: 3px 0;
  border-radius: 4px;
  box-sizing: border-box; /* パディング込みで幅いっぱい */
  background-color: #57587e;
  color: #ffffff;
}

/* ボタン全体 */
.modal-buttons-sub {
  display: flex;
  flex-direction: column;  /* 縦並び */
  align-items: center;     /* 横方向の中央揃え */
  width: 100%;             /* 親の幅いっぱいに */
}

/* ボタン全体 */
.modal-buttons-sub {
  display: flex;
  flex-direction: column;  /* 縦並び */
  align-items: center;     /* 横方向の中央揃え */
  width: 100%;             /* 親の幅いっぱいに */
}

/* リスケボタン / 変更ボタンの共通スタイル */
.modal-buttons-reschedule button {
  width: 74%;            /* 親に合わせて横いっぱい */
  margin: 0px;
  padding: 3px 50px;
  border-radius: 4px;
  box-sizing: border-box; /* パディング込みで幅いっぱい */
  border: 1px solid #000000;
  background: #e9e9e9f8;
  color: #000000;
  margin-bottom: 4px;
  cursor: pointer;        /* 活性時はマウスカーソルを手に */
  transition: background 0.2s, color 0.2s; /* スムーズ切替 */
}

/* ボタンが非活性（disabled）の時 */
.modal-buttons-reschedule button:disabled {
  background: #cccccc;    /* グレーっぽく */
  color: #666666;         /* 文字色も薄く */
  border: 1px solid #999999;
}

.hidden-calendar-input-reschedule {
  position: absolute;
  opacity: 0;
  pointer-events: none;
  height: 0;
  width: 0;
}

/* =========================
   削除アイコン（左上）
========================= */
#modal-delete {
  position: absolute;
  top: 12px;
  left: 12px;
  z-index: 10;
}

/* ==============================
   reward専用
================================ */

/* タイトル（choreと同じ見た目） */
.modal-reward-title {
  font-size: 15px;
  font-weight: 500;
  font-weight: bold;
  margin: 0;
  padding-bottom: 8px;
}

/* コスト表示 */
.reward-cost {
  display: flex;
  flex-direction: column;  /* ← 縦並びにする */
  align-items: center;
  gap: 4px;
  font-size: 14px;
}

.reward-cost-value {
  display: flex;
  align-items: center;
  gap: 4px;
  padding-bottom: 15px;
}

/* ケーキアイコン */
.reward-stamp-icon {
  width: 50px;
  height: 24px;
  display: block;
}

.member-tabs {
  display: flex;
  margin-bottom: 16px;
  padding: 6px 12px;
  background: #ffffffdc;
  text-decoration: none;
  border-radius: 8px;
}

.member-tab.active {
  background: #3030304b;
}

.member-tabs::-webkit-scrollbar {
  display: none;
}

.user-avatar-achievement {
  width: 34px;
  height: 34px;
  padding: 6px 6px;
  border-radius: 50%;
  object-fit: cover;
  line-height: 1;
  margin: 0;
  display: inline-block;
}

/* =========================
   所持ステータスバー
========================= */
.status-bar{
  width:360px;
  height:50px;
  margin:12px auto 20px;
  padding:10px 16px;

  background:#ffffff;
  border-radius:4px;

  display:flex;
  justify-content:center;
  align-items:center;
}

.status-item{
  display:flex;
  align-items:center;
  gap:8px;
}

/* アイコン */
.status-icon{
  width:60px;
  height:auto;
}

/* ラベル */
.status-label{
  font-size:15px;
  color:#555;
  font-weight:500;
}

/* 数字（ここだけ強調） */
.status-count{
  font-size:20px;
  font-weight:700;
  color:#222;
}

/* =========================
   スタンプ帳タイトル
========================= */
.stampbook-title {
  text-align: center;
  font-size: 20px;
  margin-bottom: 10px;
  font-weight: 600;
  color: #444;
}

/* =========================
   スタンプ帳台紙
========================= */
.stamp-board{
  max-width:420px;
  margin:20px auto;
  padding:32px;

  background-image: url("/assets/stampsheet.png");
  background-size: contain;
  background-repeat:no-repeat;
  background-position:center;

  display:grid;
  grid-template-columns:repeat(5,1fr);
  gap:16px;
}

/* スタンプ共通 */
.stamp{
  width:60px;
  height:60px;
  border-radius:50%;
  display:flex;
  align-items:center;
  justify-content:center;
  position:relative;
}

/* 取得済み */
.stamp.active{
  background: #ffffff;
  border:1px solid #dadada;
  box-shadow:
  0 3px 6px rgba(0,0,0,0.2);
}

/* スタンプ画像 */
.stamp-img{
  width:80%;
}

/* 未取得 */
.stamp.empty{
  background: rgba(199, 199, 199, 0.35);
}

.stamp-number{
  font-size:16px;
  color:#ffffff;
  font-weight:400;
  font-family: "Noto Sans JP", "Yu Gothic", sans-serif;
}

/* =========================
   スマホ対応
========================= */

@media (max-width: 480px) {

  .stamp-board {
    gap: 14px;
    padding: 18px;
  }

  .stamp {
    width: 60px;
    height: 60px;
  }

}

.reward-container {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 24px;

  margin-top: 40px;
  margin-bottom: 30px;

  position: relative;
}

.reward-box {
  width: 420px;
  height: 130px;
  padding: 14px 40px;

  box-sizing: border-box; /* ← これが肝 */
  display: flex;
  justify-content: space-between;
  align-items: center;

  color: #ffffff;


  background-image: url("<%= asset_path 'thankyou.jpg' %>"); /* 画像パス */
  background-size: cover;
  background-position: center;

  border-radius: 12px;
  border: 1px solid #e0e0e0;
  box-shadow: 0 6px 18px rgba(0, 0, 0, 0.08);
}

/* 左側 */
.reward-left {
  display: flex;
  flex-direction: column;
  justify-content: space-between;
}

.reward-title {
  font-size: 19px;
  font-weight: 700;
  padding-bottom: 8px;
}

.reward-need {
  font-size: 14px;
  color: #ffffff;
}

/* 右側 */
.reward-right {
  display: flex;
  align-items: center;
}

/* 丸表示 */
.reward-circle {
  width: 70px;
  height: 70px;
  border-radius: 50%;

  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;

  font-size: 14px;
  font-weight: 600;
  text-align: center;
}

/* 未達成 */
.reward-circle.ng {
  background: #fff3f3;
  color: #e05353;
  border: 2px solid #d32f2f;
  font-size: 12px;
  font-weight: 600;
}

/* 達成 */
.reward-circle.ok {
  background: #f0ffe4;
  color: #3b3a3a;
  border: 2px solid #6f9165;
}

/* 「達成まで」だけ黒文字 */
.reward-circle.ng .reward-label {
  color: #000000;
  font-size: 12px;
  font-weight: normal;
}

.stamp-count {
  display: inline-flex;
  align-items: center;
  gap: 4px;
  font-weight: bold;
}

.mini-stamp-reward {
  height: 30px;
}

.detail-edit-link {
  display: inline-flex;   /* ← 中の詳細・編集を中央揃え */
  align-items: center;
  text-decoration: none;
}

.detail-icon,
.edit-icon,
.delete-icon {
  width: 17px;
  height: auto;
  cursor: pointer;
}

.detail-icon-chore-index,
.edit-icon-chore-index,
.delete-icon-chore-index {
  width: 17px;
  height: auto;
  cursor: pointer;
  margin-top: 3px;
}

.detail-icon-home {
  width: 16px;
  height: 16px;
  display: block;
  padding-top: 4px;
}

.edit-icon-reward {
  width: 16px;
  height: 16px;
  display: block;
  padding-bottom: 5px;
}

.icon-separator {
  font-size: 16px;
}

.stamp-wrapper {
  position: relative;
  display: inline-block;
}

.stamp-icon.whole {
  width: 200px;
  padding-left: 15px;
}

.stamp-info {
  position: absolute;
  background: #fff;
  width: 90px;
  height: 90px;
  border-radius: 50%;
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  font-size: 13px;
  box-shadow: 0 4px 10px rgba(0,0,0,0.15);
  text-align: center;
}

.stamp-info.top-right {
  top: 20px;
  right: 40px;
}

.stamp-info .number {
  font-weight: bold;
  color: #e74c3c; /* 赤系でケーキ感 */
}

.stamp-count-plain {
  position: absolute;
  bottom: 10px;
  right: 25px;
  display: flex;
  flex-direction: column; /* 縦並び */
  align-items: flex-end;  /* 右寄せ */
  gap: 4px;
  font-size: 14px;
  color: #333;
}

.stamp-count-label {
  font-size: 13px;
}

.stamp-count-body {
  display: flex;
  align-items: center;
  gap: 6px;
}

.mini-stamp {
  width: 28px;
  height: auto;
}

.stamp-count-body .count {
  font-weight: bold;
}

.form-wrapper-chore {
  display: flex;
  justify-content: center;
  align-items: center;
  padding: 30px 0;
}

.form-card {
  background: #ffffff;
  width: 420px;                /* 少し広げる */
  padding: 36px 32px;
  border-radius: 12px;         /* 角丸を少し大きく */
  border: 1px solid #dcdfe3;
  box-shadow: 0 8px 24px rgba(0, 0, 0, 0.06); /* うっすら影 */
}

.form-title {
  font-size: 18px;
  font-weight: 600;
  margin-bottom: 28px;
  padding-bottom: 12px;
  border-bottom: 1px solid #e5e7eb;
  color: #333;
}

/* グループ間余白を増やす */
.form-group {
  margin-bottom: 26px;
}

.form-label {
  display: inline-block;
  font-size: 12px;
  font-weight: 600;
  margin-bottom: 8px;
  color: #6b7280; /* 少し薄く */
  letter-spacing: 0.05em;
}

.label-row {
  display: flex;
  align-items: center;
  gap: 8px;
}

.required {
  display: inline-block;
  padding: 2px 4px;
  font-size: 10px;
  color: #c62828;
  background: #fdecea;
  border-radius: 4px;
  margin-bottom: 6px;
}

.form-input,
.form-textarea {
  width: 100%;
  padding: 9px 10px;
  border-radius: 4px;
  border: 1px solid #cfd3d8;
  background: #ffffff;
  font-size: 14px;
  box-sizing: border-box;   /* ← これを追加 */
}

.form-input:focus,
.form-textarea:focus {
  outline: none;
  border-color: #5b7dbd;
}

.form-input::placeholder,
.form-textarea::placeholder {
  color: #9aa0a6;
}

/* show専用の表示フィールド */
/* 表示フィールド */
.display-field {
  background-color: #f9fafb;
  padding: 12px 14px;
  border-radius: 8px;
  border: 1px solid #e5e7eb;
  font-size: 15px;
  color: #111827;
  pointer-events: none;
}

/* textarea表示用 */
.form-textarea.display-field {
  min-height: 110px;
  line-height: 1.6;
}

.calendar-btn {
  width: 100%;
  padding: 9px 10px;
  border-radius: 4px;
  border: 1px solid #cfd3d8;
  background: #ffffff;
  font-size: 14px;
  color: #333;
  cursor: pointer;
  text-align: left;
}

.calendar-btn:hover {
  background: #f3f4f6;
}

.form-actions {
  margin-top: 30px;
  display: flex;
  justify-content: space-between; /* ← 左右に分ける */
  align-items: center;
}

.form-actions-show {
  display: flex;
  justify-content: flex-end;  /* ← これに変更 */
}

.submit-btn {
  padding: 12px 34px;  /* 横幅はpaddingで調整 */
  border-radius: 6px;
  border: none;
  background: #f3b769;
  color: #313131;
  font-size: 14px;
  font-weight: 600;
  cursor: pointer;
}

.submit-btn:hover {
  opacity: 0.9;
}

.back-link {
  display: inline-block;
  padding: 12px 14px;   /* width削除 */
  border-radius: 6px;
  border: 1px solid #d1d5db;
  background: #ffffff;
  font-size: 14px;
  color: #374151;
  text-decoration: none;
  transition: all 0.2s ease;
}

.back-link:hover {
  background: #f3f4f6;
}

/* 外側の箱 */
.home-links-box {
  position: fixed;       /* ← 位置固定 */
  bottom: 80px;          /* 下からの距離 */
  left: 50%;
  transform: translateX(-50%);  /* 中央寄せ */

  width: 300px;

  display: flex;
  flex-direction: column;
  gap: 12px;             /* ← ボタン間の隙間 */
}

/* 各ボタン */
.home-link-item {
  display: flex;
  align-items: center;
  justify-content: center;

  padding: 14px 18px;

  /* グラデーションで立体感 */
  background: #f3b769;

  border-radius: 6px;

  text-decoration: none;
  color: #333;

  /* 立体感 */
  box-shadow:
    inset 0 1px 0 rgba(255,255,255,0.15), /* 上の光 */
    0 2px 4px rgba(0,0,0,0.18);           /* 下の影 */

  transition: all 0.15s ease;
}

/* テキスト */
.link-text {
  font-size: 16px;
  font-weight: 500;
  margin-right: 10px;
}

/* 右の丸 */
.link-arrow {
  display: flex;
  align-items: center;
  justify-content: center;

  width: 22px;
  height: 22px;
  border-radius: 50%;

  background-color: #f1f1f1;
  color: #333;

  font-size: 14px;
  font-weight: bold;
  line-height: 1;
}

.chore-create-btn {
  position: fixed;      /* 画面固定 */
  right: 40px;          /* 右から */
  bottom: 70px;         /* 下から */

  width: 60px;
  height: 60px;
  border-radius: 50%;

  background-color: #ffc04c;
  color: white;

  display: flex;
  align-items: center;
  justify-content: center;

  font-size: 28px;
  font-weight: bold;
  text-decoration: none;

  box-shadow: 0 4px 10px rgba(0,0,0,0.2);
  transition: transform 0.2s, box-shadow 0.2s;
}

.field-error-message {
  color: #e53935;
  font-size: 13px;
  margin-top: 6px;
}

.input-error {
  border: 2px solid #e53935;
  background-color: #fff5f5;
}

.calendar-btn.input-error {
  border: 2px solid #e53935 !important;
  background-color: #fff5f5;
}

.devise-links-vertical {
  margin-top: 24px;
  display: flex;
  flex-direction: column;
  gap: 12px;
}

.devise-link-item {
  display: block;
  text-align: center;
  padding: 10px;
  border-radius: 8px;
  text-decoration: none;
  font-size: 14px;
  background-color: #d6d6d6;
  color: #333;
  transition: 0.2s ease;
}

.omniauth-btn {
  border: none;
  cursor: pointer;
}

/* 共通 */
.flash-message {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  padding: 16px 0;
  text-align: center;
  font-weight: bold;
  z-index: 9999;

  transform: translateY(-100%);
  opacity: 0;
  transition: all 0.5s ease;
}

/* 表示状態 */
.flash-show { 
  transform: translateY(0); 
  opacity: 1; }

/* 成功 */
.flash-notice {
  background-color: #d4edda;
  color: #1e8035;
}

/* 失敗 */
.flash-alert {
  background-color: #f8d7da;
  color: #a02632;
}

.today-chore-schedule {
  display: flex;         
  align-items: center;
  gap: 6px;
  font-size: 16px;
  margin-bottom: 6px;
  align-self: flex-start; 
  font-weight: 500;
  color: #333;
}

.yesterday-chore-schedule {
  display: flex;          
  align-items: center;
  gap: 6px;
  font-size: 16px;
  margin-bottom: 6px;
  align-self: flex-start;  
  font-weight: 500;
  color: #333;
  margin-top: 26px;
}

.checklist-icon {
  width: 26px;
  height: 26px;
  object-fit: contain;   /* ← 形を崩さない */
  flex-shrink: 0;
}

.no-chore-message {
  text-align: center;
  margin-top: 20px;
  font-size: 15px;
  color: #464646;
}

/* 空状態カード */
.empty-chore-card {
  background: #ffffff;
  border-radius: 4px;
  padding: 20px;
  margin-top: 4px;
  text-align: center;
  box-shadow: 0 2px 6px rgba(0,0,0,0.05);
}

/* メッセージ */
.empty-message {
  font-size: 14px;
  color: #464646;
  margin-bottom: 16px;
  margin-top: 0px;
}

.form-description {
  font-size: 14px;
  color: #444444;
  margin-top: 4px;
  margin-left: 57px;
}

.font-weight-bold {
  font-weight: 800;
}

.selector-buttons {
  display: flex;
  flex-direction: column;
  gap: 16px;
}

.secondary-btn {
  background-color: #d6d6d6;   /* 少し淡い色 */
  color: #333;
}

.secondary-btn:hover {
  opacity: 0.9;
}

.selection-title-user {
  display: flex;
  align-items: center;   /* 縦中央揃え */
  gap: 12px;
  margin-bottom: 14px;
}

.selection-title {
  display: flex;
  align-items: center;   /* 縦中央揃え */
  gap: 12px;
  margin-bottom: 14px;
  margin-top: 40px;
}

.user-avatar {
  width: 30px;
  height: 30px;
}

.form-selection-title {
  font-size: 16px;
  font-weight: bold;
  color: #333;
  margin: 0;   /* ← 余計なズレ防止 */
}

.file-name {
  margin-top: 8px;
  font-size: 13px;
  color: #666;
}

/* =========================
   サイドメニュー本体
========================= */
.side-menu {
  position: fixed;
  top: 0;
  left: -260px;
  width: 240px;
  height: 100%;
  background: #1e1f23;   /* 暗め背景 */
  box-shadow: 4px 0 15px rgba(0,0,0,0.4);
  transition: left 0.3s ease;
  z-index: 1001;
  padding-top: 60px;   /* 上に余白 */
}

/* 開いた状態 */
.side-menu.active {
  left: 0;
}

/* =========================
   メニューリンク
========================= */
.side-menu a {
  display: flex;              /* ← 横並びにする */
  align-items: center;
  padding: 16px 22px;
  text-decoration: none;
  color: #dcdcdc;
  font-size: 15px;
  font-weight: 500;
  transition: 0.2s;
  border-radius: 8px;
  margin: 6px 12px;
}

/* アイコン部分 */
.menu-icon {
  margin-right: 14px;
  font-size: 18px;
  width: 26px;               /* 揃える */
  text-align: center;
}

.menu-avatar {
  width: 23px;
  height: 23px;
  border-radius: 50%;
  object-fit: cover;
}

.menu-avatar-square {
  width: 23px;
  height: 23px;
  object-fit: cover;
}

/* hover時 */
.side-menu a:hover {
  background: rgba(255, 255, 255, 0.06);
  color: #ffffff;
}

/* ログアウトを少し目立たせる */
.side-menu a:last-child {
  margin-top: 20px;
  border-top: 1px solid rgba(255,255,255,0.08);
  padding-top: 20px;
}

/* =========================
   オーバーレイ
========================= */
.overlay {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background: rgba(0, 0, 0, 0.55);
  backdrop-filter: blur(2px);
  display: none;
  z-index: 1000;
}

.overlay.active {
  display: block;
}

/* =========================
   ハンバーガー強調
========================= */
.hamburger {
  cursor: pointer;
}

.hamburger .footer-icon {
  font-size: 20px;
}

.category-wrapper {
  padding: 30px;
  max-width: 1000px;
  margin: 0 auto;
}

.category-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 25px;
}

.category-add-btn {
  background: #f3b769;
  color: white;
  padding: 8px 16px;
  border-radius: 20px;
  text-decoration: none;
  font-size: 15px;
  font-weight: 600;
}

.category-grid {
  display: grid;
  grid-template-columns: repeat(3, 160px);
  gap: 20px;
}

.category-card {
  position: relative;
  background: #ffffff;
  border-radius: 16px;
  padding: 25px 10px;
  text-align: center;
  text-decoration: none;
  color: #333;
  box-shadow: 0 4px 10px rgba(0,0,0,0.08);
  transition: 0.2s ease;
}

.category-icon {
  width: 74px;
  height: 74px;
  margin: 0 auto 12px;

  border-radius: 50%;          /* 丸くする */
  border: 2px solid #e0e0e0;   /* 外枠 */
  background: #fff;            /* 背景を白に */
  
  display: flex;
  align-items: center;
  justify-content: center;

  overflow: hidden;            /* はみ出し防止 */
}

.category-icon img {
  max-width: 95%;     /* ← 少し小さめでスタンプ感 */
  max-height: 95%;
  object-fit: contain; /* 切らずに収める */
}

.category-name {
  font-size: 14px;
  font-weight: 600;
  padding: 4px 8px;
  border-radius: 8px;
  display: inline-block;
}

.category-empty {
  text-align: center;
  margin-top: 40px;
  color: #777;
}

.card-main-link {
  display: block;
  text-decoration: none;
  color: inherit;
}

.delete-link {
  position: absolute;
  top: 8px;
  left: 8px;
  z-index: 10;
}

.color-options {
  display: flex;
  gap: 12px;
  flex-wrap: wrap;
  margin-top: 8px;
}

.color-choice {
  position: relative;
  cursor: pointer;
}

.color-choice input {
  position: absolute;
  opacity: 0;
}

.color-circle {
  width: 28px;
  height: 28px;
  border-radius: 50%;
  border: 2px solid #ddd;
  display: inline-block;
  transition: 0.1s;
}

/* 選択時 */
.color-choice input:checked + .color-circle {
  border: 2px solid #7389c5;
  transform: scale(1.1);
}